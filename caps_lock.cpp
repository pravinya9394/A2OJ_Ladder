#include<iostream>
#include<string>
using namespace std;
int main()
{
    string s;
    cin>>s;
    bool flag=1;
    if(s.size()==1&&s[0]==s[0].tolower())
    {
        cout<<s.to
    }
    for(auto it:s)
    {
        if(it)
    }
    return 0;
}